# Архитектура рекомендательной системы

## 4. Диаграмма компонентов приложения


```mermaid
graph TD
    A[REST Controller] --> B[RecommendationsService]
    B --> C[RecommendationRuleSet implementations]
    B --> D[TransactionRepository]
    D --> E[JdbcTemplate]
    E --> F[H2 Database]
    C --> D


## 5. Диаграмма поведения (Activity Diagram)

flowchart TD
    A[Получение user_id из запроса] --> B{Валидация UUID}
    B -- Некорректный --> C[Возврат ошибки 400]
    B -- Корректный --> D[Запрос данных транзакций пользователя]
    D --> E[Проверка правил для Invest 500]
    E --> F{Правила выполнены?}
    F -- Да --> G[Добавление Invest 500 в рекомендации]
    F -- Нет --> H[Проверка правил для Top Saving]
    H --> I{Правила выполнены?}
    I -- Да --> J[Добавление Top Saving в рекомендации]
    I -- Нет --> K[Проверка правил для Простого кредита]
    K --> L{Правила выполнены?}
    L -- Да --> M[Добавление Простого кредита в рекомендации]
    L -- Нет --> N[Формирование ответа]
    G --> N
    J --> N
    M --> N
    N --> O[Возврат JSON с рекомендациями]
